# api/

Thin serving layer between `fusion/`+`anomaly/` and `web/`: exposes the
current fused picture, track history, and the live anomaly feed. Deliberately
not the interesting part of this project — keep it simple, don't let it eat
time that belongs in `fusion/`.
